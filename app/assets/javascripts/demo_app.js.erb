$(function(){
   sortData();
   $('#percent_correct').on('click', sortPercentCorrect);
    $('#number_completed').on('click', sortNumberComplete);
    $('#tab_grid').on('click', 'dd', getinfo);
});

function deleteClass(){
  $('#tab_grid dd').removeClass('active')
}

function getinfo(){

  var attr = $(this).attr('id');
  console.log(attr);
  deleteClass('active');
  $('#' + attr).addClass('active')
  var classroom = $('#container1').data('classroom')
  $.ajax({
    dataType: 'script',
    type: 'GET',
    url: '/classrooms/getinfo',
    data: { attr: attr, classroom: classroom }
  }).done();

  return false;

}


function setLayout(){
  $('#container1').isotope({ layoutMode : 'fitRows' });
}

function sortData(){
   $('.container').isotope({
    getSortData : {
    percentage_correct : function ( $elem ) {
      console.log($elem.find('.percentage_correct').text());
      return parseInt( $elem.find('.percentage_correct').text(), 10 );
      },

      symbol : function ( $elem ) {
      return $elem.find('.number_complete').text();
      }
    }
  });
}

  function sortPercentCorrect(){
    console.log('hello');
  $('.container').isotope({ sortBy : 'percentage_correct', sortAscending : false });
  }

  function sortNumberComplete(){
    console.log('helloone');
  $('.container').isotope({ sortBy : 'number_complete', sortAscending : false });
  }



// $(function(){
//   $('.vertical.tabs').on('click', 'dd', find_content);

// });

// function find_content()
// {
//   var selected = $(this).text();
//   render_content(selected);
// }

// function render_content(selected)
// {
//   if(selected == "Percent Correct")
//     {
//     $('.topic').hide();
//     $('.time_per_lecture').hide();
//     $('.percent_correct').show();
//   }
//   else if(selected == "Topic")
//     { $('.topic').show();
//       console.log('topic');
//     $('.percent_correct').hide();
//     $('.time_per_lecture').hide();
//      }
//   else if(selected == "Time per lecture")
//     $('.time_per_lecture').show();
//     $('.percent_correct').hide();
//     $('.topic').hide();
// }

// $('#container').isotope({
//   getSortData : {
//     number : function ( $elem ) {
//     return parseInt( $elem.find('.name').text(), 10 );
//   }
// });

// $('#container').isotope({ sortBy : 'name' });