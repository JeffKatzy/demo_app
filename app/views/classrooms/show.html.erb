
<!-- End Navigation -->

  <!-- Side Bar -->

        <div class="three mobile-four columns">

          <!-- <img src="http://placehold.it/500x500&text=Logo"> -->

          <div class="hide-for-small panel">

            <h5 class="subheader">Risus ligula, aliquam nec fermentum vitae, sollicitudin eget urna. Donec dignissim nibh fermentum odio ornare sagittis.
            </h5>
          </div>

          <a href=<%= edit_classroom_path(@classroom)%>>
          <div class="panel callout radius" align="center">
            <h6>Edit Classroom Info</h6>
          </div>
          </a>

        </div>

    <!-- End Side Bar -->


    <!-- Thumbnails -->
<dl class="tabs contained nine columns">
  <dt> Student Performance </dt>
  <dt><%= link_to "Add new student", new_user_path %></dt>
  <dd class="active tab"><a href="#simpleContained1">Current</a></dd>
  <dd class="hide-for-small tab"><a href="#simpleContained2">Today</a></dd>
  <dd class="hide-for-small tab"><a href="#simpleContained3">Overall</a></dd>
</dl>
<ul class="tabs-content contained nine columns">
  <li class="active" id="simpleContained1Tab">
    <div class="row">
      <dl class="vertical tabs pill two columns">
        <dd class="active"><a href="#vertical1" class="percent_correct">Percent Correct</a></dd>
        <dd><a href="#vertical2" class="number_completed">Number Completed</a></dd>
        <dd><a href="#vertical3">Time per lecture</a></dd>
      </dl>

      <div class="container" id="conatainer1">
        <% @users.each do |user| %>
          <% current_lecture = user.user_lectures.current.last %>
          <% if current_lecture.present? %>
            <div class="percent_correct element <%= evaluate(user.current_percentage_correct) %>">
                <h6 class="symbol"><%= name_number(user) %></h6>
                <h6 class="name"><%= current_lecture.user_answers.percent_correct %></h6>
                <h6 class="lecture name"><%= user.user_lectures.current.last.try(:lecture).try(:name) %></h6>
                <h6 class="number"><%= user.name %></h6>
                <h6 class="number_complete"> user.user_answers_current_count </h6>
            </div>
          <% else %>
            <div class="percent_correct element <%= evaluate(user.current_percentage_correct) %>">
                <h6 class="symbol"><%= name_number(user) %></h6>
                <h6 class="name"><%= user.lecture_percentage_correct_today %></h6>
                <h6 class="number_complete"> <%= user.user_answers_current_count %></h6>
            </div>
          <% end %>
        <% end %>
      </div>
  </li>

  <li class="active" id="simpleContained2Tab">
    <div class="row">
          <dl class="vertical tabs pill two columns">
            <dd class="active"><a href="#vertical1" class="percent_correct">Percent Correct</a></dd>
            <dd><a href="#vertical2" class="number_completed">Number Completed</a></dd>
            <dd><a href="#vertical3">Time per lecture</a></dd>
          </dl>

          <div class="container" id="container2">
            <% @users.each do |user| %>
              <% lectures = user.user_lectures.today %>
              <% if lectures.present? %>
                <div class="percent_correct element <%= evaluate(user.lecture_percentage_correct_today) %>">
                    <h6 class="symbol"><%= name_number(user) %></h6>
                    <h6 class="name"><%= user.lecture_percentage_correct_today %></h6>
                    <h6 class="number_complete">  <%= user.user_answers_today_count %></h6>
                </div>
              <% else %>
                <div class="percent_correct element <%= evaluate(user.lecture_percentage_correct_today) %>">
                    <h6 class="symbol"><%= name_number(user) %></h6>
                    <h6 class="name"><%= user.lecture_percentage_correct_today %></h6>
                    <h6 class="number_complete"> <%= user.user_answers_today_count %></h6>
                </div>
              <% end %>
            <% end %>
          </div>
      </div>
  </li>

  <li class="active" id="simpleContained3Tab">
    <div class="row">
      <dl class="vertical tabs pill two columns">
        <dd class="active"><a href="#vertical1" class="percent_correct">Percent Correct</a></dd>
        <dd><a href="#vertical2" class="number_completed">Number Completed</a></dd>
        <dd><a href="#vertical3">Time per lecture</a></dd>
      </dl>

        <div class="container" id="container3">
          <% @users.each do |user| %>
            <% lectures = user.user_lectures %>
            <% if lectures.present? %>
              <div class="percent_correct element <%=  evaluate(user.lecture_percentage_correct) %>">
                <h6 class="symbol"><%= name_number(user) %></h6>
                <h6 class="name"><%= user.lecture_percentage_correct %></h6>
                <h6 class="number_complete"> <%= user.user_answers.try(:count) %></h6>
              </div>
            <% else %>
              <div class="percent_correct element <%= evaluate(user.lecture_percentage_correct) %>">
                <h6 class="symbol"><%= name_number(user) %></h6>
                <h6 class="username"><%= user.name %></h6>
                <h6 class="name"><%= user.lecture_percentage_correct %></h6>
                <h6 class="number_complete"> <%= user.user_answers.try(:count) %></h6>
              </div>
              <% end %>
            <% end %>
          </div>
      </div>
  </li>


    <!-- End Managed By -->

          </div>
        </div>
      </div>
