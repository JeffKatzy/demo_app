<% provide(:title, @teacher.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @teacher.name %>
      </h1>
      <% @teacher.classrooms.each do |classroom| %>
      	<%= link_to classroom.name, classroom, class: "btn" %>
      <% end %>
    </section>
  </aside>
</div>
